{
  "name": "gatherly",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,backend -c green,blue \"npm run dev --workspace @gatherly/frontend\" \"npm run dev --workspace @gatherly/backend\"",
    "build": "npm run build --workspace @gatherly/frontend && npm run build --workspace @gatherly/backend",
    "typecheck": "npm run typecheck --workspace @gatherly/frontend && npm run typecheck --workspace @gatherly/backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
