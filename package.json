{
  "name": "gatherly",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c green,blue \"npm run dev --workspace @gatherly/web\" \"npm run dev --workspace @gatherly/api\"",
    "build": "npm run build --workspace @gatherly/web && npm run build --workspace @gatherly/api",
    "typecheck": "npm run typecheck --workspace @gatherly/web && npm run typecheck --workspace @gatherly/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
